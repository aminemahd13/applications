{
    "name": "@event-platform/db",
    "version": "0.0.1",
    "private": true,
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "scripts": {
        "build": "tsc",
        "generate": "prisma generate",
        "db:pull": "prisma db pull",
        "seed": "ts-node seed.ts",
        "seed:prod": "prisma migrate deploy --schema prisma/schema.prisma && ts-node seed.ts"
    },
    "dependencies": {
        "@prisma/client": "^5.0.0"
    },
    "devDependencies": {
        "prisma": "^5.0.0",
        "ts-node": "^10.9.2",
        "typescript": "^5.0.0"
    }
}
